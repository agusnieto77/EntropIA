# Linux native lib fixtures

These files are small audit fixtures so runtime-pack packaging and CI can verify Linux resource wiring in-repo.
Real release jobs must inject the actual audited shared libraries before shipping.
