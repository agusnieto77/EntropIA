# Keep the pnpm lockfile byte-stable across runners
pnpm-lock.yaml text eol=lf

# Runtime-pack fixture manifests validate file bytes; keep hashed text payloads stable on Windows runners.
apps/desktop/src-tauri/resources/runtime-pack/**/scripts/*.py text eol=lf
apps/desktop/src-tauri/resources/runtime-pack/**/wheelhouse/*.txt text eol=lf
